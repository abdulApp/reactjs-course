# Context API

Context API is a feature that allows to manage and
share state across the component tree without having to
pass props down manually at every level. It's useful for
scenarios where it needs to share data or functions
across mant components, especially when these
components are deeply nested.